import type { Metadata } from "next";
import { CaseStudyShell } from "@/components/SiteShell";

export const metadata: Metadata = {
  title: "Google Cloud Security Engineering",
  description: "Terraform-based Google Cloud security engineering case study covering keyless IAM, network segmentation, Cloud Audit Logs, VPC Flow Logs, Cloud KMS, protected log archives and evidence-led validation.",
};

export default function GcpCloudSecurityCaseStudy() {
  return <CaseStudyShell><main><section className="case-hero section-dark"><div className="container case-hero-grid"><div><p className="eyebrow">CASE STUDY 07 • TERRAFORM SECURITY ENGINEERING</p><h1>Google Cloud Security Engineering</h1><p>A Terraform-based Google Cloud security reference environment designed around keyless identity, segmented networking, protected security telemetry, encryption and repeatable control validation.</p><div className="case-cta"><a className="button button-primary" href="https://github.com/KaffyDevelops/gcp-cloud-security-engineering" target="_blank" rel="noreferrer">View GitHub repository ↗</a></div></div><aside className="case-facts"><div><small>FOCUS</small><strong>Cloud Security Engineering</strong></div><div><small>PLATFORM</small><strong>Google Cloud</strong></div><div><small>TOOLS</small><strong>Terraform • Cloud Logging • KMS • IAM</strong></div><div><small>STATUS</small><strong>Baseline built • runtime validation pending</strong></div></aside></div></section><section className="case-body"><div className="container case-layout"><nav className="case-nav"><a href="#problem">Problem</a><a href="#identity">Identity</a><a href="#network">Network</a><a href="#evidence">Evidence security</a><a href="#assurance">Assurance</a><a href="#validation">Validation</a></nav><article className="case-content"><section id="problem" className="case-section"><p className="eyebrow dark">SECURITY PROBLEM</p><h2>A cloud project needs more than enabled APIs and administrator access.</h2><p>The security problem is to make identity, network and telemetry boundaries explicit while avoiding long-lived credentials, unnecessary public exposure and weak evidence handling. The project therefore treats authentication, segmentation, logging, encryption and validation as one engineering system.</p><div className="case-note"><strong>Evidence state:</strong> the architecture, Terraform, threat model and automated quality controls are implemented. Google Cloud runtime controls are not labelled validated until observed evidence is captured in an authorised lab project.</div></section><section id="identity" className="case-section"><p className="eyebrow dark">KEYLESS IAM</p><h2>Separate identity creation from privilege, and avoid private service-account keys by default.</h2><p>The baseline creates dedicated application, data and Terraform deployment service accounts without creating user-managed private keys. Workload Identity Federation is available as an opt-in path so GitHub Actions can eventually obtain short-lived Google Cloud credentials through OIDC rather than storing a service-account JSON key.</p><div className="case-matrix"><div><small>WORKLOADS</small><strong>Dedicated identities</strong><p>Application and data tiers use separate service accounts instead of a shared runtime identity.</p></div><div><small>CI/CD</small><strong>GitHub OIDC</strong><p>Workload Identity Federation is the target deployment authentication model.</p></div><div><small>PRIVILEGE</small><strong>Explicit grants</strong><p>Creating a service account does not automatically grant broad project permissions.</p></div><div><small>EVIDENCE</small><strong>Auditable actions</strong><p>Validation will confirm whether federated deployment activity is attributable in Cloud Audit Logs.</p></div></div></section><section id="network" className="case-section"><p className="eyebrow dark">NETWORK SECURITY</p><h2>Build intentional paths and make them observable.</h2><p>The custom-mode VPC contains separate web, application and data subnets. Private Google Access is enabled, VPC Flow Logs are configured on all three subnets, firewall logging is enabled, web-to-app and app-to-data paths are explicit, and internet-origin SSH/RDP is denied by policy.</p><p>The base project does not create VMs, public IP addresses, Cloud NAT or load balancers simply to make the diagram look more complex.</p></section><section id="evidence" className="case-section"><p className="eyebrow dark">AUDIT & EVIDENCE SECURITY</p><h2>Security telemetry should survive long enough to support investigation and assurance.</h2><p>Cloud Audit Logs, VPC Flow Logs and firewall telemetry are routed through a Cloud Logging sink into a dedicated Cloud Storage archive. The archive uses uniform bucket-level access, public-access prevention, versioning, retention and a customer-managed Cloud KMS key with rotation.</p><p>Data Access audit logging is deliberately opt-in because useful security telemetry also has volume, privacy and cost consequences.</p></section><section id="assurance" className="case-section"><p className="eyebrow dark">AUTOMATED ASSURANCE</p><h2>Let CI challenge the architecture before a cloud deployment does.</h2><p>The GitHub Actions quality workflow runs Terraform formatting, provider initialisation and `terraform validate`, then executes repository-specific security guardrails that reject service-account key resources, legacy public bucket ACL patterns and missing baseline controls.</p><p>The first CI run caught an incorrect Google provider resource declaration for the Cloud Storage service account. The implementation was corrected to use the provider data source, and the second full quality run passed. That history is retained as engineering evidence rather than hidden.</p></section><section id="validation" className="case-section"><p className="eyebrow dark">VALIDATION PROGRAMME</p><h2>Use one GCP lab to strengthen infrastructure evidence and detection engineering together.</h2><p>The backlog covers cost-safe deployment, network segmentation, VPC Flow Logs, Cloud Audit Log archiving, KMS and storage protection, keyless IAM, GitHub Workload Identity Federation, Data Access audit logs, Security Command Center assessment, protected Terraform state and controlled teardown.</p><p>A dedicated cross-repository validation issue will also generate authorised Google Cloud IAM telemetry for the `GCP-001` and `GCP-002` rules in the Detection as Code repository. This creates a stronger evidence chain than treating each project as an isolated demo.</p><div className="case-note">Organisation-level controls such as VPC Service Controls and some Security Command Center capabilities remain explicitly design/backlog items until the required hierarchy, tier and runtime evidence exist.</div></section></article></div></section></main></CaseStudyShell>;
}
